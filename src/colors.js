// For  defining the  light and dark color

const ColorInit = {
    // Light Theme
    light: {
        textColor: "#000",
        backgroundColor: "#fff",
        border: "2px solid #000"
    },
    
    // Dark Theme
    dark: {
        textColor: "#fff",
        backgroundColor: "#333", 
        border: "2px dashed #000"
    },
};

export default ColorInit;