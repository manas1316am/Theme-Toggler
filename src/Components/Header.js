import React from "react"

const Header = () => {
    return (
        <header>
            <h1 
                style = {{
                    fontWeight: "bold",
                    fontSize: "40px",
                    textAlign: "center"
                }}
            >
                Theme Toggler
            </h1>
        </header>
    );
};

export default Header;